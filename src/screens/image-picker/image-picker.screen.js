import React from "react";
import { View, Text } from "react-native";

export const ImagePicker = () => {
  return (
    <View
      style={{
        width: "100%",
        height: "100%",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <Text>This will be the image picker screen.</Text>
    </View>
  );
};
